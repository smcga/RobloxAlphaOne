local Constants = {
    SCORE_INTERVAL_SECONDS = 1,
    SCORE_INCREMENT = 1,
    ZONE_NAME = 'ScoreZone',
    ZONE_CENTER = Vector3.new(0, 1, 0),
    ZONE_RADIUS = 14,
    ZONE_HEIGHT = 4,
    BASEPLATE_SIZE = Vector3.new(128, 1, 128),
}

return Constants
